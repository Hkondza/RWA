@model List<JobFinder.WebApp.ViewModels.Application.JobApplicationListVM>

<h2>Job Offers</h2>

@if (!Model.Any())
{
    <p>Nema dostupnih oglasa.</p>
}
else
{
    <table style="width:100%; border-collapse:collapse; margin-top:20px;">
        <thead>
            <tr>
                <th style="text-align:left;">Firma</th>
                <th>Tip</th>
                <th>Status</th>
                <th>AppliedAt</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var job in Model)
        {
            <tr style="border-bottom:1px solid #ddd;">
                <td>@job.FirmName</td>
                <td>@job.JobName</td>
                <td>@job.Status</td>
                <td>@job.AppliedAt.ToString()</td>
                <td>
                    <a href="/JobOffer/Details/@job.IDJobOffer">Details</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
